[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"
dockerBuildTarget = "api"

[deploy]
# Railway's public `Neumas` API service is currently attached to this manifest.
# Let the Docker image CMD start Gunicorn/Uvicorn for the HTTP service.
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
