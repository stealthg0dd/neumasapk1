web: bash start.sh
worker: bash -c 'echo "[neumas-worker] REDISHOST=${REDISHOST:-<unset>} REDISPORT=${REDISPORT:-<unset>}" >&2 && export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}$(pwd)/neumas-backend" && cd neumas-backend && exec celery -A app.core.celery_app worker --loglevel=info --queues=neumas_default,scans,agents,neumas.predictions,neumas.shopping --concurrency=${CELERY_CONCURRENCY:-2} --pool=prefork'
